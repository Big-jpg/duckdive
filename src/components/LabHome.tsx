"use client";
/* eslint-disable @next/next/no-html-link-for-pages */
import {useEffect,useState} from "react";import {useRouter} from "next/navigation";
type Dive={key:string;title:string;label:string;description:string;diveId:string;session:string;accent:string};
export default function LabHome(){const router=useRouter(),[dives,setDives]=useState<Dive[]>([]),[error,setError]=useState("");useEffect(()=>{fetch("/api/gallery",{cache:"no-store"}).then(async response=>{const body=await response.json();if(!response.ok)throw new Error(body.error||"Gallery unavailable");setDives(body.dives);}).catch(reason=>setError(reason.message));},[]);async function remix(dive:Dive){const response=await fetch("/api/edit",{method:"POST",headers:{"Content-Type":"application/json"},body:"{}"});if(!response.ok){setError("Could not prepare your editor workspace.");return;}router.push(`/edit?key=${dive.key}`);}return <main className="lab-shell"><header className="lab-header"><a href="/" className="lab-brand"><span className="lab-duck">D</span><span>DUCKDIVE <i>GOLD</i></span></a><nav><a href="#dives">Dives</a><a href="/api/analytics/suburbs">Data API</a><button onClick={async()=>{await fetch("/api/auth/logout",{method:"POST"});location.reload();}}>Sign out</button></nav></header><section className="lab-hero"><div><p className="lab-kicker">88,422 source rows · 83 source files · VIC</p><h1>House data,<br/><em>made explorable.</em></h1><p>Private, interactive analytics built from an auditable sales estate. Open a Dive to explore or remix it with AI.</p><div className="lab-actions"><a href="#dives" className="lab-primary">Explore the Dives ↓</a><a href="https://motherduck.com/product/dives/" target="_blank" rel="noreferrer">What is a Dive? ↗</a></div></div><aside><span>Coverage</span><strong>2004—2026</strong><p>Detached-house observations across the supplied Victorian collection. Descriptive history, not a valuation.</p></aside></section><section id="dives" className="lab-dives"><div className="lab-section-title"><p>03 live experiments</p><h2>Choose your way in.</h2></div>{error&&<div className="lab-error" role="status">{error}</div>}{!error&&!dives.length&&<div className="lab-loading">Preparing live Dives…</div>}<div className="lab-gallery">{dives.map((dive,index)=><article key={dive.key} className={`lab-card accent-${dive.accent}`}><div className="lab-preview">{dive.session?<iframe title={`${dive.title} preview`} src={`https://embed-motherduck.com/sandbox/#session=${dive.session}`} sandbox="allow-scripts allow-same-origin"/>:<span>Loading live preview…</span>}</div><div className="lab-card-body"><span>0{index+1} / {dive.label}</span><h3>{dive.title}</h3><p>{dive.description}</p><div><button onClick={()=>router.push(`/dives/${dive.key}`)}>Open Dive ↗</button><button onClick={()=>remix(dive)}>Remix with AI ✦</button></div></div></article>)}</div></section><footer className="lab-footer"><strong>DUCKDIVE.GOLD</strong><p>VIC House Data · Immutable CSV → Neon → MotherDuck</p><p>Not affiliated with realestate.com.au</p></footer></main>}
